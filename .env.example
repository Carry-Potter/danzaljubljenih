# Lokalno (SQLite) – zameni provider u prisma/schema.prisma na "sqlite"
# DATABASE_URL="file:./dev.db"

# Vercel / produkcija (PostgreSQL) – npr. Neon
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
